package Week0;

public record Notes() {
    // Create new project using the New>Project>Maven type
    // Create new java files under main>java>java class or under a new package (folder essentially)
    // Designate the used package, then designate a public class titled after the java class you created
    // Finally designate the private static void using the "main" java environment

    //package Week0;
    //public class PizzaRolls {
        //public static void main(String[] args) {
        //}
    //}

    // Remember to use sout - which is a shortcut for system.out.println for easy printing
    // Remember escape characters for formating \, \n, \t (Escape, new line, tab)
    // Remember formatting characters %s, %d, %f (String, Variable, Float/Double) For different data types
    // Can use %.2f to limit the float to 2 decimal places
    // When using % formatting characters, follow up the ("%s %d example text %f") format with the values you want to insert
    // Use commas (,) to separate each added segment of the formatting print line
    // Remember to use souf - shortcut for system.out.printf for formatting
    // Can concatenate strings with + like in any other language

}
